{
    "builds": [
      {
        "src": "app/api/app.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "app/api/app.py"
      }
    ],
    "installCommand": "pip install llama-index==0.12.32 && pip install -r requirements.txt"
  }
  
